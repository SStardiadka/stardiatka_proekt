import time

start = time.time()

# res = 0
a = map(lambda x: str(x) , range(100000000))
    # res += i

print(time.time()-start) # время работы программы
# start = time.time()

# # res = 0
# A = [int(i) for i in range(100000000)]
#     # res += i
# print(time.time()-start)