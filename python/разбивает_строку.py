# Метод partition()

# """Метод partition() разбивает строку по заданной подстроке.
# После этого результат возвращается в виде кортежа.
# При этом подстрока, по которой осуществлялась разбивка, тоже входит в кортеж."""

str = "givethhiismaaanaguun"
# srt =
print(str)
a = str.partition("hh")
print(a)
